import { Text, View } from "@/components/theme/Themed"

const ChannelListWeb = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Stream chat not available on web right now</Text>
    </View>
  );
};

export default ChannelListWeb;
